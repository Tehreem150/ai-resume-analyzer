{
  "name": "ai-resume-analyzer",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "formidable": "^3.5.4",
    "pdf-parse": "^1.1.1"
  }
}
